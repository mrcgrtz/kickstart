
/*! normalize/.styl | http://github.com/nulltask/normalize/.styl */
/*! based on hail2u's normalize/.scss | https://github.com/hail2u/normalize/.scss */

normalize-html5()
  @import 'vendor/normalize/html5'

normalize-base()
  @import 'vendor/normalize/base'

normalize-links()
  @import 'vendor/normalize/links'

normalize-typography()
  @import 'vendor/normalize/typography'

normalize-lists()
  @import 'vendor/normalize/lists'

normalize-embed()
  @import 'vendor/normalize/embed'

normalize-figures()
  @import 'vendor/normalize/figures'

normalize-forms()
  @import 'vendor/normalize/forms'

normalize-tables()
  @import 'vendor/normalize/tables'

global-normalize()
  normalize-html5()
  normalize-base()
  normalize-links()
  normalize-typography()
  normalize-lists()
  normalize-embed()
  normalize-figures()
  normalize-forms()
  normalize-tables()

